{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope={@boilerplate/common,@boilerplate/server}",
    "build:web": "lerna run build --scope={@boilerplate/common,@boilerplate/web}"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "prettier": {
    "singleQuote": true
  }
}
